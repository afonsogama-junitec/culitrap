# Dependências para CuliTrap - Armadilha Culicoides
# Nota: picamera2 deve ser instalado via apt (sudo apt install python3-picamera2)

ultralytics>=8.2.0        # YOLOv11/YOLOv26 inferência e treino
sahi>=0.11.14             # Sliced inference para deteção de insetos pequenos
opencv-python-headless    # Processamento de imagem (sem GUI)
pandas                    # CSV/JSON para logs de deteções
matplotlib                # Visualização de gráficos
numpy                     # Cálculos matemáticos (essencial para OpenCV/YOLO)
